"""Forge — AI-operated content venture.

Separate, clean project. Reuses the battle-tested titan faceless-video
pipeline (sibling repo) via `forge.bootstrap`, which loads titan's secrets
and re-exports the render/publish/asset entry points.
"""

__version__ = "0.1.0"
